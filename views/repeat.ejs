<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To-Do List</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/static/css/styles.css">
</head>
<body class="d-flex flex-column min-vh-100">
    <!-- navbar -->
    <%- include('navbar.ejs') %>
    <main class="container py-5 flex-grow-1">
        <section class="todo-list">
            <div class="row justify-content-center">
                <div class="col-lg-8">
                    <!-- task form -->
                    <div class="task-form card mb-4 shadow-sm">
                        <div class="card-body">
                            <form class="d-flex gap-2" action="/" method="post">
                                <input type="text" name="taskAdd" class="task-card form-control" placeholder="Add new task..." required>
                                <button type="submit" class="btn btn-dark text-white">
                                    <i class="bi bi-plus-lg"></i> Add
                                </button>
                            </form>
                        </div>
                    </div>
                    <% if(tasks.length==0){ %>
                        <!-- (when no tasks) -->
                        <div class="text-center text-white py-5 d-block">
                            <i class="bi bi-check2-circle display-4 text-muted mb-3"></i>
                            <h5 class="text-muted">No tasks yet</h5>
                            <p class="text-muted">Add your first task using the form above</p>
                        </div>
                    <% }else{ %>
                        <% for(var i=0; i<tasks.length; i++){%>
                        <!-- Task Cards -->
                        <div class="task-form card mb-3 shadow-sm">
                            <div class="task-card card-body d-flex align-items-center">
                                <div class="form-check flex-grow-1 d-flex align-items-center gap-2">
                                <input class="form-check-input" type="checkbox" id="task<%=i+1%>">
                                <label class="form-check-label task-label" for="task<%=i+1%>"><%= tasks[i]%></label>
                                </div>
                                <div class="btn-group">
                                    <!-- <form action="/edit" method="get"> -->
                                        <button name="editButton" class="btn btn-sm btn-outline-info">
                                            <i class="bi bi-pencil"></i>
                                        </button>
                                    <!-- </form> -->
                                     <form action="/delete" method="post" class="btn btn-sm btn-outline-danger">
                                         <button name="deleteButton" class="btn btn-sm btn-outline-danger">
                                             <i class="bi bi-trash"></i>
                                         </button>
                                     </form>
                                </div>
                            </div>
                            <div class="card-footer text-muted small">
                                Created: Date/Day, Time
                            </div>
                        </div>
                        <% } %>
                    <% } %>
                </div>
            </div>
        </section>
    </main>
    <!-- footer -->
    <%- include('footer.ejs') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>





<!-------------------- extras -------------------->


<!-- <%= if(task.length()==0){ %> -->
<!-- <%= }else{ %> -->
<!-- <%= } %> -->



<!-- <%= for(var i=0; tasks<tasks.length; i++){%>
<%=     tasks[i]; %>
<%= } %> -->

<!-- app.js -->
<!-- app.post("/delete",(req,res)=>{
  // var valueToRemove = req.body.deleteButton;
  // var index = tasks.indexOf(valueToRemove);
  // if(index>-1){
  //   tasks.splice(index, 1);
  // }
  console.log(req.body.deleteButton);
  res.redirect("/");
}); -->


<!-- navbar -->
        <!-- <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse fs-5" id="navbarContent">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" href="edit.ejs">
                        <i class="bi bi-pencil-square me-1"></i>Edit
                    </a>
                </li>
            </ul>
        </div> -->